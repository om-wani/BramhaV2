# Archived Full-Platform Docs

These are the full-vision architecture and phase specs for the original BramhaV2 platform. They describe a more complex system than the current MVP target. The codebase at that point is preserved at git branch `v0-full-platform`.

New MVP docs live at `docs/` (root level). Start there.
